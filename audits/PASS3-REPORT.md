# Pass 3 — The look, elevated
**Executed 2026-09-20. Edits in `app/src/app.jsx` and `app/index.html` only (index.html owns the desktop-width CSS), plus `tests/formulas.test.mjs` and the `app/sw.js` cache bump. Never touched `app/app.js`. Line references are post-edit.**

**Verification used throughout:** after every batch — `tests/formulas.test.mjs` (**53 passed, 0 failed** final: 49 from Pass 2 + 4 new dedupe assertions); full-file `@babel/parser` parse (JSX plugin) — PARSE OK; full compile with `@babel/core` + `@babel/preset-react` to a scratch file — COMPILE OK (647 KB); compiled bundle evaluated end-to-end in Node with stubbed React/DOM/localStorage — EVAL OK (all components define, boot path runs, render is called). The browser pane cannot reach this sandbox's files, so **no screenshot pass was possible** — the "could NOT be visually verified" notes below are the honest residue; live screenshots happen after deploy.

---

## 1. Brand marks as components — DONE
- The six hand-cut marks from `brand/assets/marks/mark-*.svg` transcribed faithfully into app.jsx as `M.tree`, `M.bucket`, `M.tubing`, `M.evaporator`, `M.jug`, `M.sugarhouse` (app.jsx:744–809), each `({size=48, color='currentColor'})` via a shared `Mark` wrapper (48×48 viewBox, `style={{color}}` so `currentColor` bodies follow the prop). Teal `#2DD4A7` sap-drop accents (`MARK_TEAL`) and `fillRule="evenodd"` holes preserved exactly; SVG comments stripped; kebab-case attributes converted to JSX camelCase (`fill-rule`→`fillRule`, `stroke-width`→`strokeWidth`, `stroke-linecap`→`strokeLinecap`) — path data byte-identical to the asset files.
- Placed AFTER the `I` icon library and BEFORE the `// ─── Formulas` banner, deliberately outside the test-extracted band (JSX cannot run in the test sandbox's `new Function`).
- `M.tree`, `M.bucket`, `M.jug`, `M.evaporator` are in use (below); `M.tubing` and `M.sugarhouse` are defined and ready (Tubing's empty state is already the benchmark and was preserved untouched; sugarhouse is landing-page material).
- Verified: parse + compile + eval. **NOT visually verified:** the marks' rendered appearance in-app (they are the same paths already verified at 120/64/32 in the Sept 19 asset session per brand/BIBLE.md §5b).

## 2. Desktop shell — DONE
- **app.jsx (App, :10514–10546):** a new `<nav className="side-nav">` renders inside `.app-header`, listing ALL FIVE destinations (Today, Log, Bush, Numbers, Shed) as vertical items — stroke icon (`d.Icon`, 20px, teal when active) + label — with the active destination's sub-screens nested beneath it (`.side-subs`/`.side-sub`, active = teal left rule) and a persistent teal **"Log a run"** button (`.side-log`, `margin-top:auto` so it pins to the sidebar's foot). It navigates to Log and fires a `sr-log-a-run` window event 150 ms later; LogTab (:3462–3469) listens and opens the entry sheet directly, saving the second tap. Stroke icons, not marks, in the nav: at 20 px the marks are below their 32 px floor (BIBLE §5b) — nav is control tier.
- **index.html:** the desktop band is retargeted `min-width:1100px` → **`1024px`** and rebuilt (:444–560). `.side-nav { display:none }` outside the band, so the five destination buttons now EXIST in the DOM at desktop width (they were the mobile bottom bar's only, computed `display:none` — Cal's #1 defect). At ≥1024px: `.tab-bar-wrap` (mobile's horizontal sub-tab strip) is `display:none` (the sidebar nests sub-screens instead), sidebar width 224px, all items ≥44px hit areas, hover states (`rgba(255,255,255,0.03)` wash + text lift), active teal left-rule + tint, `:focus-visible` rings on all three control classes.
- **Mobile (<1024px) unchanged by construction:** the only rule touching mobile is `.side-nav { display:none }`; the bottom bar, sub-tab strip, and every other band (≤767, 768–1023) are byte-identical except the band edge moving 1100→1024 (1024–1099 was previously *tablet* layout with the bottom bar; it is now the fixed desktop shell — that slice is the deliberate change, not a regression).
- Verified: by reasoning over both breakpoints against the stylesheet + DOM structure, parse/compile/eval. **NOT visually verified:** actual rendering at 1440×900 and 1024×768; the 150 ms mount race on the sidebar Log-a-run (worst case: user lands on Log with the button visible, one extra tap); sidebar scroll behavior when all sub-screens of Numbers are open on a short viewport (`overflow-y:auto` on `.app-header` should handle it).

## 3. Furnished empty states — DONE (Tubing's standard, Tubing untouched)
- **Today first-run** (`entries.length === 0`, :9605–9622): centered `M.bucket` at 96 in amber `#EB9A33`, the existing season-goal card and heading kept above, one-line sub (`todayEmptySub`), and three tappable 44px starter chips — **Log first sap** → `go('log')`, **Set my location** → `go('weather')`, **Plan my season** → `go('tapping')` — using the same `go` (=`setTab`) mechanism the pinned CTA already uses. The pinned "Log a run" bar stays.
- **Log › Entries empty** (:3541–3556): `M.jug` at 72 amber, the existing `noEntries` string as title, sub (`logEmptySub`), and a "Log a run" chip that opens the entry sheet. The redundant full-width primary bar is skipped in exactly this case (chip carries the action); it still renders for the filtered-empty collection-point case and whenever entries exist.
- **Shed › Equip empty** (:4074–4081): `M.evaporator` at 72 amber above the existing `noEquipYet`/`equipDesc` copy (the third wrench glyph in one viewport — Cal's note — is gone).
- **Bush › Map › Trees empty (no pins)** (:6159–6166): `M.tree` at 72 amber + new title **"Drop your first pin" / «Plantez votre premier repère»** (`dropFirstPin`), existing "Tap the map in Tap Tree mode…" line kept.
- **Day-one grade:** when `seasonScore` returns grade `'—'`, SweetRunScore (Recap, :8725–8738) now renders a calm **"Too early to grade / Trop tôt pour évaluer"** line (+ sub: "The score gets its letter once syrup is logged.") instead of the number row, and hides "Share score" (it would have shared "(—)"). SeasonIntelligence's grade tile (:7611–7618) goes neutral gray (no red border, `tooEarlyGrade` caption instead of the %) when ungraded. The scoring model itself untouched.
- **Translations:** 9 new keys added to BOTH tables (:373–384 EN, :636–647 FR), Québécois voice matched (coulée, sève, repère, bilan de saison). No existing string changed or removed. SweetRunScore gained a `lang` prop (RecapTab passes it); SeasonIntelligence reads `sg_lang` for its one new string.
- Verified: parse/compile/eval; every referenced key greped present in both tables. **NOT visually verified:** spacing/balance of the mark modules at 375px and desktop; chip wrap on narrow phones (flex-wrap + 44px min-height should hold).

## 4. SugarSage answers — DONE
- **The Ask control never disappears** (:8258–8285): while text is present the primary action stays a green **Ask** submit button; clear is now a separate small × (`type="button"`, 32px box, right of center at `right:66`) — previously the submit button was *replaced* by the clear-X, leaving a form with no submit.
- **Enter submits:** the form now always contains exactly one `type="submit"` button, so implicit submission fires `handleSubmit` → `doSearch` reliably.
- **Results render where you're looking** (:8321–8390): ranked entries (`ssSearch` keyword search, unchanged) now render **directly under the category chips**; the Season Dashboard, Break-Even calculator, and source note step aside while `displayList.length > 0` and return on clear (`clearSearch` resets everything). No more answers buried below two screens of dashboard — the dead-end is gone in both directions.
- The rotating prompt line is untouched.
- Verified: parse/compile/eval + code-path reasoning (debounced auto-search also still runs; submit gives instant results). **NOT visually verified:** the two-button layout inside the input at 375px (input padding widens to 104px when text is present to make room for both).

## 5. CSV + import safety (Debug M3/M4) — DONE
- **CSV export** (:3298–3311): now exports **all six entry kinds** — Fuel and Hours included — with a `Unit` column (fuel in the operation's fuel unit, hours in `hr`, volumes in the display unit) and RFC-4180 quote doubling so a note containing `"` can't break its row. Header `Type,Date,Value,Unit,Note` (was `Type,Date,Value (gal),Note` — which was wrong for litre users and for fuel anyway). Type labels via the existing `t(lang, kind)` keys.
- **Import dedupe** (Debug M4): new pure `dedupeImport(existingSlog, additions)` + `srEntryKey` in the formula band (:997–1015, inside the test-extracted region) — an incoming entry is a duplicate iff **kind + date + value** match an entry already in the store (numeric value compare, `'100' ≡ 100`; matching is against the existing store only, so a file that legitimately contains two identical rows still imports both the first time). `SapImportModal` builds its additions once (`buildAdditions`, :3031–3049) shared by a memoized preview report and `doImport` (:3052–3084), so preview and import can never disagree. Preview shows **"{n} new · {m} duplicates will be skipped"** (amber, only when m>0), the confirm button counts only what will actually be added, and a fully-duplicate re-import disables the button with "Already imported — 0 new entries". EN + FR (`importDupNote`, `importAllDup`); modal now receives `lang` from LogTab.
- **Tests:** 4 new assertions on the dedupe matcher (skip exact kind+date+val; keep the rest; same date+val under another kind is NOT a dup; numeric value comparison). Suite: **49 → 53**.
- Verified: tests + parse/compile/eval. **NOT visually verified:** the modal's preview strip with a real re-imported PDF (logic is test-locked; the copy path is plain JSX).

## Housekeeping
- `app/sw.js` cache bumped `sweetrun-v15` → `sweetrun-v16` (once, at the end).
- `app/app.js` NOT touched (stale generated file; Cloudflare builds from source; owner's local `npm run build` reconciles). No new dependencies. All six ARCH invariants hold: no hand-edit of app.js; all data stays under `sg_*` (no new keys written at all this pass); SW bumped; formula band remains the single source of truth (dedupeImport added TO it, tested); `ls.set` untouched; no locale-formatted dates added.
- Preserved untouched per the brief: the copy/voice (only additions, no rewrites), the data palette on data, Boil Pt's amber card, the wizard flow and its teal selection color, Tubing's benchmark empty state.

## Needs eyes-on after deploy (consolidated)
1. Desktop shell at 1440 and 1024: sidebar layout, active/hover states, Log-a-run pin, no double-nav at any width, 1024–1099 slice.
2. The four mark-furnished empty states + Today's starter chips at 375px.
3. SugarSage: type → Enter → results directly beneath; × clears; dashboard returns.
4. Recap/SugarSage "Too early to grade" on a fresh profile with sap-only logged.
5. Import a SugarCalc PDF twice: second pass must show the skip note and a disabled button.
